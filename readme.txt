in order to run shell you need to download original SPIM package and replace syscall files. 

Command are given in shell.asm 


This projects adds manual context switching on to SPIM via new system calls.